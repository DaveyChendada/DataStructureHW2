#include "queue.h"
#include "timer.h"
#include <assert.h>
#include <stdlib.h>
#include <stdio.h>
#include <bits/stdc++.h>
#include <map>


int
main (int argc, char* argv[])
{
	int n = stoi(argv[1], 0, 10);
	int t = stoi(argv[2], 0, 10);
	cout<<"n="<<n<<endl;
	cout<<"t="<<t<<endl;
}